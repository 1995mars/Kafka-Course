<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>vn.motdev</groupId>
    <artifactId>kafka-streams-demo</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>kafka-streams-demo</name>
    <description>Demo Buổi 13: pipeline 3 microservice với Spring Cloud Stream + Kafka Streams</description>

    <!-- Multi-module đúng như slide "Ứng dụng demo: 3 microservice" — mỗi module
         một ứng dụng Spring Boot độc lập, nối nhau qua 2 topic -->
    <modules>
        <module>number-producer</module>
        <module>number-processor</module>
        <module>number-consumer</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <!-- Spring Cloud 2023.0.x (Leyton) là release train tương thích Boot 3.3 -->
        <spring-cloud.version>2023.0.5</spring-cloud.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
